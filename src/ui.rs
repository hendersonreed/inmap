use crate::Config;

pub fn draw_ui(line: &String, config: &Config) -> String {
    // write the preview function
    // write all possible execution lines
    // blinking cursor to demonstrate anticipated input (stretch goal)
    // upon a keypress, clear the screen, return keypress.
    "unimplemented".to_string()
}
